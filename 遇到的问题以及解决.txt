UserServlet问题：
	1.BeanUtils.populate(user, parameterMap);封装失败 
		原因：jsp页面name属性和实体不对应，id没有写
			id和name必须一样，而且和实体对应	
			form表单提交隐藏域中name属性，对应的值是value
	2.表查询的时候sql语句出错
	3.标签页没有写fade出错
		bootstrap中要跳转需要加上fade（fade只是表示有个渐变效果...）
	4.MD5加密两次出错
		解决方法：检查浏览器中的cookie值和数据库是否对应
					不对应肯定是加密出错
OrderServlet
	1.页面加载场馆类别的时候ajax需要用.append引入，而不是.html
	2.页面加载的时候，onclick时间里面绑定场馆类型名冲突出错。
		原因还不清楚：可能是id冲突将数据库里面加一个字段可能会解决
		通过this获取到a标签，然后获得a标签的内容就是场馆类型，就可以通过
		场馆类型和当前时间去查找 
	3.页面加载的时候，因为自己设置的是onclick事件，导致页面加载完毕后不能第一时间去查找默认的
		场馆类型（乒乓球场），所以需要自己设置默认值
			解决方法1：直接在findVenueByIdAndTime(id)中判断传递过来是不是空，是空就调用函数，不是就直接
						失败原因：就算是空值，我也是绑定的点击事件，没有点击不会执行
			解决方法2：直接在页面给场馆变量赋值，发现赋值如果没有判断还是失败
			解决方法3：在ajax方法中调用findVenueByIdAndTime(id)，传递一个“'#乒乓球场'”进去，发现没有办法获取
						应该是异步的问题，所以换成$.ajax({});方法，传递进去获得的是全部的信息
			解决方法4：通过id获得this属性document.getElementById('乒乓球场')，用原生js可以获得
						但是在function()后要判断一下request域里面标志是否为空，如果为空，表示第一次访问过去
						第一次访问的时候，request还没有返回过来，就执行下面的函数findVenueByIdAndTime(id)去调用下面的方法了。
			post是不行的。ajax异步同步都可以 （懵了）
	4.通过findVenueByIdAndTime(id)查找表单的时候，内容写错，然后上面的场馆类型也加载不出来了
	5. get方式提交request.setCharEncoding();没用
	6.Gson转日期出现中文   换用fastjson就行了
	7.设置前台属性的时候，先看一下表里面的字段再设计，避免重复改代码
	8.点击到另外一页清除原来的样式再添加新的table
	9.当选择了当前页面后，跳到篮球场继续选，因为选择了所以会失败，回来后绿色消失了，没办法点，但是信息已经记录到input里面了
		解决方案：通过获取特定id判断，如果是和当前一样，就回显，但是自己代码一直放到ajax后面，就一直异步。
					我认为是还没有加载完毕就执行了if里面的方法 给他加了个绿色。异步加载的问题，放到ajax里面判断就没问题了
			


需要解决的问题(全部以解决)
1.页面的alert() 和确认confirm
2.当前时间，什么时候订单完成 超过这个时间点改成灰色 监听器
3.订单生成时间问题
5.通知公告
6.打印报表

http://121.36.38.15:8080/GMS/

ALTER USER 'root'@'localhost' IDENTIFIED BY '123456';
